import { atom } from 'recoil';

export const navMoState = atom({
  key: 'navMoState',
  default: false,
});

export const navEqState = atom({
  key: 'navEqState',
  default: false,
});

export const navPrState = atom({
  key: 'navPrState',
  default: false,
});

export const navNoState = atom({
  key: 'navNoState',
  default: false,
});

export const navWoState = atom({
  key: 'navWoState',
  default: false,
});
